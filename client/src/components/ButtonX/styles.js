import styled from 'styled-components'
import { Link } from 'react-router-dom'

export const StyledButtonX = styled(Link)`
	position: absolute;
	right: 15px;
	cursor: pointer;

	svg {
		font-size: 1.5rem;
	}
`
